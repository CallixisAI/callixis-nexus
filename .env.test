# Loaded automatically by Vitest (mode "test") so importing modules that create
# the Supabase client doesn't throw in unit tests. These are fake and safe to
# commit — no test touches a real Supabase project.
VITE_SUPABASE_URL=https://test.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=test-anon-key
